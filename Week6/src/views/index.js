import React, {Components} from 'react'
import {
  View,
  Text,SafeAreaView
} from 'react-native'
import Page from './page'
export default RootComponent = () => {
  return(
    <SafeAreaView>
    <Page />
    </SafeAreaView>
  );
}